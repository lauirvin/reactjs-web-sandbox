import styles from './styles.module.scss';

const CanvasDrawingBoard = (): JSX.Element => {
  console.log('hi');
  return <div className={styles.container}>Canvas Drawing Board</div>;
};

export default CanvasDrawingBoard;
